<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mp.alex.majorprojectmk2">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.planetsubadapter.PlanetSub"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.itineraryadapter.ItineraryMain" />
        <activity android:name=".ui.planitinadapter.ItinerarySub" />
        <activity android:name=".ui.ItineraryCreate" />
        <activity android:name=".ui.planetadapter.SearchResult" />
        <activity android:name=".ui.SearchNew" />
    </application>

</manifest>